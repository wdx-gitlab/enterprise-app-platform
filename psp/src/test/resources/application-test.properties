# authz-engine 测试专用配置
# 指向唯一迁移目录，TestApplication 排除了自定义 AuthzEngineAutoConfiguration，
# 由 Spring Boot 默认 FlywayAutoConfiguration 在 H2 数据源上执行迁移以创建测试所需的表
spring.flyway.locations=classpath:db/authz-migration
spring.flyway.baseline-on-migrate=true
